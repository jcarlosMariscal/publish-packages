{
  "name": "@jcmariscal/demo-vite",
  "private": false,
  "version": "0.2.4",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "release": "release-it",
    "copy-meta": "cp package.json dist/ && cp CHANGELOG.md dist/ && cp README.md dist/",
    "prepare-publish": "npm run release && npm run copy-meta",
    "publish:dist": "cd dist && npm publish"
  },
  "devDependencies": {
    "typescript": "~5.8.3",
    "vite": "^7.0.4"
  }
}
